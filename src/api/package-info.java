/**
 * This package contains the API interfaces and classes for the Pacman game.
 * It defines the core abstractions and contracts that the implementation must follow.
 * 
 * The API includes:
 * - Game entities (Actor, Descriptor, Location)
 * - Game state (Mode, Direction, CellType)
 * - Game mechanics (MazeMap, MazeCell)
 * - Game controller (PacmanGame)
 */
package api;